# Audience Targeter 
def target_audience(demographics, preferences):
    # Fungsi untuk menargetkan audiens yang tepat
    # Bisa menggunakan data demografis, preferensi, perilaku, dll.
    targeted_groups = "Audiens yang tertarget"  # Contoh sederhana
    return targeted_groups

if __name__ == "__main__":
    demographics = {...}
    preferences = {...}
    audience = target_audience(demographics, preferences)
    print(f"Targeted Audience Groups: {audience}")
